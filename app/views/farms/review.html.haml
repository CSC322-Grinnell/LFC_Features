-# This file is app/views/farms/review.html.haml
%h1 Review for Approval

- if @no_invalid_farms
  %p="There are no farms pending approval"

%table#invalid_farms
  %thead
    %tr
      %th Farm Name
  %tbody
    - @invalid_farms.each do |farm|
      %tr
        %td= farm.name

= link_to 'Back to index', index_path
